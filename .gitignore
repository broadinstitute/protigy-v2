# R package files
.DS_Store
.Rhistory
.RData
Rplots.pdf
.Rproj.user
.Rapp.history

# RStudio Connect
rsconnect/

# Documentation images
# inst/doc_images/workflow_diagram.png  # Commented out so image appears on GitHub
.claude/
.cursor/

CLAUDE.md

# Pak lockfiles (optional, platform-specific)
pkg.lock
.github/pkg.lock

.worktrees/

firebase-debug.log

docs/
.positai

# PELSA: regenerable UniProt annotation outputs (users rebuild via Setup page refresh).
# FASTA search databases + schema.json ARE tracked; the derived caches are not.
inst/database/*/uniprot_features/*.tsv
inst/database/*/uniprot_features/*.parquet
inst/database/*/uniprot_membrane/*.tsv
inst/database/**/.DS_Store
