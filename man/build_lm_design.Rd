% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers.R
\name{build_lm_design}
\alias{build_lm_design}
\title{Build the LM design matrix: single source of truth for preview and fit}
\usage{
build_lm_design(
  cdesc,
  formula_string,
  variable_types = list(),
  reference_levels = list(),
  blocking_var = NULL
)
}
\arguments{
\item{cdesc}{Sample metadata \code{data.frame} (a GCT \code{cdesc}); row names are
sample ids.}

\item{formula_string}{Model formula string; \code{""}/\code{NA}/\code{NULL} is allowed only
when \code{blocking_var} is set (repeated measures without groups).}

\item{variable_types}{Named list mapping variable names to \code{"factor"} or
\code{"continuous"}.}

\item{reference_levels}{Named list mapping factor variables to a chosen
reference level; unknown variables/levels are ignored.}

\item{blocking_var}{Optional blocking variable name (a column in \code{cdesc}).}
}
\value{
A list with elements: \code{design} (numeric matrix or \code{NULL} on error),
\code{cdesc_clean} (filtered metadata or \code{NULL}), \code{n_used}, \code{n_total},
\code{dropped} (count), \code{repeated_measures_only} (logical),
\code{warnings} (character vector), and \code{error} (message string or \code{NULL}).
}
\description{
Constructs the design matrix exactly as \code{\link[=lm.regression]{lm.regression()}} does, so the
on-screen design preview (in \code{tab_lm_setup}) and the actual fit can never
disagree. It performs, in order: formula normalization (empty formula plus a
blocking variable becomes intercept-only \code{~ 1}), per-variable coercion and
reference releveling, complete-case sample dropping over the formula
variables \emph{and} the blocking variable, \code{droplevels} with a single-level
guard, repeated-measures detection, and a rank-deficiency check.
}
\details{
Diagnostics are returned as DATA, never signalled: \code{error} is a message
string when no usable design can be built (or \code{NULL} on success), and
\code{warnings} is a character vector of non-fatal notices (rank deficiency,
missing/degenerate blocking). The preview renders these; \code{\link[=lm.regression]{lm.regression()}}
re-raises them via \code{stop()}/\code{warning()}.
}
