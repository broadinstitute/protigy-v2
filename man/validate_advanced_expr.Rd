% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers_contrasts.R
\name{validate_advanced_expr}
\alias{validate_advanced_expr}
\title{Validate that an advanced contrast expression references design coefs.}
\usage{
validate_advanced_expr(expr, design_coefs)
}
\arguments{
\item{expr}{Character scalar, user-supplied contrast expression.}

\item{design_coefs}{Character vector of design-matrix column names.}
}
\value{
list(ok = logical, message = character, unknown = character)
}
\description{
Returns a list with \code{ok} (logical) and \code{message} (character). Splits the
expression on operators ( + - * / ( ) ) and checks that every token is
either a numeric literal, whitespace, an operator, or a known design coef
(after \code{make.names()} normalization, matching the backend path).
}
