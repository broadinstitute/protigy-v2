% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_customize_helpers.R
\name{import_colors_from_yaml_full}
\alias{import_colors_from_yaml_full}
\title{Import colors from YAML -- full structured result}
\usage{
import_colors_from_yaml_full(file_path, custom_colors)
}
\arguments{
\item{file_path}{Path to the YAML file}

\item{custom_colors}{Current custom colors structure (to preserve structure)}
}
\value{
A list with:
\itemize{
\item \code{colors}               - updated custom_colors
\item \code{n_columns_updated}    - integer count of column entries actually changed
\item \code{n_omes_in_yaml}       - integer count of omes that matched session
\item \code{invalid_entries}      - character vector of "ome$col$val=#bad"
\item \code{missing_omes}         - omes present in YAML but not in session
\item \code{format}               - "ProTIGY" | "PANOPLY-nested" | "PANOPLY-flat" | "none"
\item \code{warnings}             - character vector of all warning messages
\item \code{skipped_continuous_function_palettes} - ome$col entries that couldn't be restored
\item \code{alpha_stripped_count} - integer; how many hex inputs had alpha stripped
}
}
\description{
Internal workhorse. Returns a structured list describing the import so the
Shiny module can surface counts, missing omes, format detected, and
normalization warnings to the user. The thin back-compat wrapper
\code{import_colors_from_yaml()} calls \code{warning()} for each entry in \verb{$warnings}
and returns just the colors list.
}
\details{
Implements a three-scenario matching algorithm:
\enumerate{
\item All conditions match: Apply colors based on condition-color name matching
\item Some conditions match: Apply colors to matches, then unused colors
sequentially (alphabetically) to unmatched conditions
\item No conditions match: Apply colors by order sequentially to conditions
}

Accepts two YAML shapes:
\itemize{
\item ProTIGY: \code{colors: { ome: { annot_col: { val: "#hex" } } }}
\item PANOPLY: \code{groups.colors: { annot_col: { val: "#hex" } }} (nested) or
\code{groups.colors: { val: "#hex" }} (flat). Both are applied to every ome
in the current session.
}

Errors (file not readable, malformed YAML) are raised via \code{stop()}.
}
