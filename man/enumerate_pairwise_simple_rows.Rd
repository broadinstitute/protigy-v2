% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers_contrasts.R
\name{enumerate_pairwise_simple_rows}
\alias{enumerate_pairwise_simple_rows}
\title{Enumerate all pairwise Simple contrast rows for a factor variable.}
\usage{
enumerate_pairwise_simple_rows(
  factor_levels,
  variable_name,
  include_intercept = TRUE
)
}
\arguments{
\item{factor_levels}{Character vector of factor levels (length >= 2).}

\item{variable_name}{Character scalar, the formula variable name (e.g.
"treatment").}

\item{include_intercept}{Logical; if FALSE the design uses a cell-means
parameterisation (\code{~ 0 + V}), in which case the design column names are
the bare level names (no \code{V} prefix). Defaults to TRUE.}
}
\value{
A list of row drafts (each itself a list). Empty list if fewer than
two levels.
}
\description{
Given the levels of a factor of interest and the design-column prefix used
for that factor (i.e. the variable name from the user's formula), returns a
list of row drafts ready to be appended to \code{contrast_rows}. Each draft is in
the same shape as a Simple-mode card: \code{id}, \code{type="simple"}, \code{num}, \code{den},
\code{advanced_expr=""}, auto-label, \code{label_user_edited=FALSE}.
}
\details{
The pairs are generated by \code{utils::combn(levels, 2)}: each unordered pair
(L_i, L_j) with i<j becomes a single contrast \code{L_i - L_j}. Users can swap
a card if they want the reversed direction.

Design-coefficient names follow R's \code{model.matrix} convention: for an
intercept-included formula the coefficient corresponding to level \code{L} of
variable \code{V} is \code{paste0(V, L)}. We use that here without consulting the
design matrix; the caller is responsible for confirming the variable is in
the active design before calling this function.
}
