% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers.R
\name{parse_interaction_terms}
\alias{parse_interaction_terms}
\title{Resolve selected interaction terms into variable pairs}
\usage{
parse_interaction_terms(selected_variables, interaction_terms)
}
\arguments{
\item{selected_variables}{Character vector of currently selected variable
names (\code{input$selected_variables}).}

\item{interaction_terms}{Character vector of checked interaction labels in the
picker's \code{"A : B"} format (\code{input$interaction_terms}); may be \code{NULL}.}
}
\value{
A list of length-2 character vectors (the chosen interaction pairs),
in \code{combn()} enumeration order. Empty list when fewer than two variables
are selected, when \code{interaction_terms} is empty, or when no label matches.
}
\description{
Turns the LM setup module's \verb{(selected_variables, interaction_terms)} input
pair into the list of interaction variable pairs consumed by
\code{\link[=build_formula_string]{build_formula_string()}} and the fit. Crucially, it is crash-proof for any
number of selected variables: the interaction picker's \code{interaction_terms}
checkbox input persists stale (non-\code{NULL}) after a variable is unchecked, so
this helper must NOT call \code{utils::combn(selected_variables, 2)} unless at
least two variables are currently selected -- \code{combn(x, 2)} errors with
"n < m" when \code{length(x) < 2}. Any stale interaction label that no longer maps
to a current pair is silently dropped.
}
