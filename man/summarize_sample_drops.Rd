% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers.R
\name{summarize_sample_drops}
\alias{summarize_sample_drops}
\title{Summarize how many samples will be dropped by \code{complete.cases} filtering.}
\usage{
summarize_sample_drops(cdesc, model_vars)
}
\arguments{
\item{cdesc}{Data frame of sample metadata (rows = samples).}

\item{model_vars}{Character vector of column names that feed
\code{complete.cases()}. Duplicates are deduped; unknown columns are silently
ignored so callers don't have to pre-filter the formula's \code{all.vars()}
output against \code{colnames(cdesc)}.}
}
\value{
Either NULL or a list with:
\itemize{
\item \code{n_total}: integer, \code{nrow(cdesc)}.
\item \code{n_used}: integer, surviving rowcount.
\item \code{n_dropped}: integer, \code{n_total - n_used}.
\item \code{dropped_columns}: character vector of column names whose NAs caused
drops, ordered by descending NA count (alphabetical tie-break). Empty
when nothing was dropped.
\item \code{message}: one-line human-readable caption suitable for display above
the design-matrix preview.
}
}
\description{
Given the working \code{cdesc} and the variables that participate in the design
(model variables plus the optional blocking variable), report how many
samples will survive the \code{complete.cases} filter and attribute the drops to
the columns that introduced NAs.
}
\details{
Returns NULL when there is nothing meaningful to report (empty data, no
inspectable variables).
}
