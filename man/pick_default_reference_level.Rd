% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers.R
\name{pick_default_reference_level}
\alias{pick_default_reference_level}
\title{Choose a defensible default reference level for a factor variable.}
\usage{
pick_default_reference_level(values)
}
\arguments{
\item{values}{Character vector or factor of observed levels (one entry per
sample). NA and empty strings are dropped.}
}
\value{
A list with:
\itemize{
\item \code{level}: chosen level (character) or \code{NA_character_} for empty input.
\item \code{reason}: one of \code{"control_token"}, \code{"modal"}, \code{"tie_alphabetical"},
\code{"single"}, \code{"empty"}.
\item \code{matched_token}: present only when \code{reason == "control_token"}; the
lowercase token from \code{.lm_control_tokens} that matched.
\item \code{n}: present only when \code{reason == "modal"}; the count for the modal level.
}
}
\description{
Replaces the alphabetical-first default (R's \code{factor()} behaviour) with a
rule chain that more often matches the scientist's mental model:
}
\details{
\enumerate{
\item If any observed level (case-insensitive) matches a control token from
\code{.lm_control_tokens}, pick that level. First match wins, by token-list
order.
\item Otherwise, pick the most-frequent (modal) level.
\item If two or more levels tie for modal, pick the alphabetical first.
}

Empty / NULL / all-NA input returns \code{NA_character_}.
}
