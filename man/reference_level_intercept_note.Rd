% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_setup_helpers.R
\name{reference_level_intercept_note}
\alias{reference_level_intercept_note}
\title{Caveat about how the intercept setting affects the chosen reference levels.}
\usage{
reference_level_intercept_note(include_intercept, n_factors)
}
\arguments{
\item{include_intercept}{Logical scalar; the state of the "Include intercept"
checkbox.}

\item{n_factors}{Integer count of factor variables in the model.}
}
\value{
Character scalar. Empty string when no caveat applies (intercept on,
or no factors).
}
\description{
Reference levels are treatment-coding baselines. When the intercept is
included (\code{~ variables}), every factor is coded against its reference, so the
choice matters for all of them and no caveat is needed. When the intercept is
dropped (\code{~ 0 + variables}), R cell-means-codes only the \emph{first} factor - its
reference merely reorders columns and changes no test - while every other
factor (and any interaction) stays treatment-coded, so their references still
matter. This helper returns the user-facing note describing that coupling.
}
