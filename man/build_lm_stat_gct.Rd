% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_lm_results_helpers.R
\name{build_lm_stat_gct}
\alias{build_lm_stat_gct}
\title{Build a GCT object from an LM results frame, ready for SSGSEA.}
\usage{
build_lm_stat_gct(df)
}
\arguments{
\item{df}{Data frame with at least an \code{id} column and one or more
\verb{logSignP.<coef>} columns.}
}
\value{
A \code{cmapR::GCT} object, or \code{NULL} if no \code{logSignP} columns exist.
}
\description{
The \verb{@mat} slot holds the \verb{logSignP.<coef>} columns (one column per
coefficient); \verb{@rdesc} carries everything else (annotations, raw sample
values, and the per-coef logFC / P.Value / adj.P.Val statistics). This
mirrors \code{R/tab_stat_summary.R:700-707} so downstream SSGSEA tooling can
consume LM outputs the same way it consumes stat-module outputs.
}
